# Balls
some experiments with AVX512 intrinsics and collision detection / resolution.

> [!NOTE]
> `libraylib.a` is compiled with Wayland support only. if you want an X11 build or a Windows build, get it from the [raylib repo](https://github.com/raysan5/raylib) and replace `thirdparty/libraylib.a` with your desired lib
